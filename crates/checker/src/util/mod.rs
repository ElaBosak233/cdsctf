//! Utility module group for `checker`.

/// Defines the `leet` submodule (see sibling `*.rs` files).
pub mod leet;

/// Defines the `suid` submodule (see sibling `*.rs` files).
pub mod suid;

/// Defines the `xxtea` submodule (see sibling `*.rs` files).
pub mod xxtea;
