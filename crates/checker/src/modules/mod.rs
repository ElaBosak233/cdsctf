//! Rune built-in module `mod` for challenge checker scripts.

/// Defines the `audit` submodule (see sibling `*.rs` files).
pub mod audit;

/// Defines the `crypto` submodule (see sibling `*.rs` files).
pub mod crypto;

/// Defines the `fs` submodule (see sibling `*.rs` files).
pub mod fs;

/// Defines the `leet` submodule (see sibling `*.rs` files).
pub mod leet;

/// Defines the `regex` submodule (see sibling `*.rs` files).
pub mod regex;

/// Defines the `suid` submodule (see sibling `*.rs` files).
pub mod suid;
