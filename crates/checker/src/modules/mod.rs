//! Lua modules exposed to challenge checker scripts.

/// Defines the `audit` submodule (see sibling `*.rs` files).
pub mod audit;

/// Defines the `fs` submodule (see sibling `*.rs` files).
pub mod fs;

/// Defines the `leet` submodule (see sibling `*.rs` files).
pub mod leet;

/// Defines the `suid` submodule (see sibling `*.rs` files).
pub mod suid;
